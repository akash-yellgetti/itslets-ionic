import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "com.itslets.app",
  appName: "ItsLets",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
